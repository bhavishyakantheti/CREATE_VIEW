# CREATE_VIEW
- CREATE VIEW is used to save a complex SQL query as a virtual table, which you can query like a regular table.
- It helps in hiding sensitive columns, simplifying queries, and improving security and abstraction.
